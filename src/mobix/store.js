import { spectrumInfo } from "./spectrum-info";
import { windowInfo } from "./window-info";
import { config } from "./config";

const store = {
  spectrumInfo,
  windowInfo,
  config,
};

export { store };
